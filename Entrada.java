package ep.ia;
/*Objeto de Entrada que possui um array de linhas que contém os 64 valores das respectivas colunas e
 * o resultado de cada linha que representa o digito escrito a mao pela pessoa.
 */
public class Entrada {
	public double[] linha;//linha contendo todos os atributos por entrada
	public int resultado;//contem a classe da linha correspondente
	
	public Entrada(double[] linha, int resultado){
		this.linha = linha;
		this.resultado = resultado;
	}
	
	
	

}
